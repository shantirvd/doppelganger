<div class="container form">
  <h1>New booking</h1>
  <%= simple_form_for [@drag, @booking] do |f| %>
    <%= f.input :start_time, as: :date %>
    <%= f.input :end_time, as: :date %>
    <%= f.input :location %>
    <%= f.submit "Book now", class: "button" %>
  <% end %>
</div>
